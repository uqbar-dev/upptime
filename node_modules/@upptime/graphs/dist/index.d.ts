type SiteConfig = {
    name: string;
    url: string;
    slug?: string;
};
export declare const getSiteSlug: (site: SiteConfig) => string;
export declare const generateGraphs: () => Promise<void>;
export {};
